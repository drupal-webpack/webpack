services:
  webpack.commands:
    class: \Drupal\webpack\WebpackDrushCommands
    arguments: [ '@module_handler', '@theme_handler','@library.discovery', '@state', '@file_system' , '@config.factory']
    tags:
    - { name: drush.command }
