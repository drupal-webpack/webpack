services:
  webpack.commands:
    class: \Drupal\webpack\WebpackDrushCommands
    arguments: [ '@module_handler', '@theme_handler','@library.discovery', '@state', '@config.factory', '@file_system' ]
    tags:
    - { name: drush.command }
