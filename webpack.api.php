<?php

/**
 * Customize the webpack config for your site.
 *
 * @param array &$config
 *   PHP representation of the webpack config objects. Functions and regular
 *   expressions can be provided as strings. Special keys:
 *   - '#lines_before' - an array os strings to print before the config export statement.
 */
function hook_webpack_config_alter(&$config) {
  // Add your custom stuff to webpack config.
}
